<?xml version="1.0" encoding="UTF-8"?>
<project name="Igefa Test Project" basedir="./" default="test">

    <!--
        Include local project properties.
    -->
    <property file="${basedir}/build.properties.local" />
    <property file="${basedir}/build.properties" />

    <!--
        Import main target defintions (extension points)
    -->
    <import optional="true" file="${basedir}/ant/main.xml" />

    <!--
        Enable used modules
    -->
    <import file="${basedir}/ant/modules/npm.xml" />
    <import file="${basedir}/ant/modules/bower.xml" />
    <import file="${basedir}/ant/modules/grunt-js-seed.xml" />
</project>
